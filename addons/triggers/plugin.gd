@tool
extends EditorPlugin

# The trigger node types are registered globally via `class_name` declarations
# in each script (e.g. `class_name Trigger`), so they appear in the "Create New
# Node" dialog with their `@icon` automatically while this plugin is enabled.


func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass
